# Environment Configuration for Kafka Collector
# Copy this file to .env and modify as needed

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
# Name of the binary executable
BINARY_NAME=collector

# ============================================================================
# KAFKA CONFIGURATION
# ============================================================================
# Kafka bootstrap servers (comma-separated list for multiple brokers)
# For KRaft mode (Kafka 4.1+), use port 9093. For legacy mode, use 9092
KAFKA_BOOTSTRAP_SERVERS=localhost:9093

# Message timeout in milliseconds
KAFKA_MESSAGE_TIMEOUT_MS=5000

# Delivery timeout in milliseconds
KAFKA_DELIVERY_TIMEOUT_MS=5000

# Request timeout in milliseconds
KAFKA_REQUEST_TIMEOUT_MS=5000

# Socket timeout in milliseconds
KAFKA_SOCKET_TIMEOUT_MS=5000

# ============================================================================
# HTTP SERVER CONFIGURATION
# ============================================================================
# Server bind address
# Use 127.0.0.1 for local access only, 0.0.0.0 for public access
HOST=127.0.0.1

# Server port
PORT=8080

# Set to 'true' to enable public access (automatically changes HOST to 0.0.0.0)
# PUBLIC_ACCESS=true

# ============================================================================
# RUNTIME CONFIGURATION
# ============================================================================
# Log level: error, warn, info, debug, trace
RUST_LOG=info

# Path to the YAML configuration file containing endpoint definitions
CONFIG_FILE=conf.yaml

# ============================================================================
# BUILD & DEPLOYMENT CONFIGURATION
# ============================================================================
# These variables are used by the build and deployment scripts

# Installation prefix for system-wide deployment
INSTALL_PREFIX=/usr/local

# Directory where the binary will be installed
INSTALL_BIN_DIR=/usr/local/bin

# Directory where configuration files will be installed
INSTALL_CONFIG_DIR=/usr/local/etc/collector

# Directory for distribution packages
DIST_DIR=dist

# ============================================================================
# SYSTEM SERVICE CONFIGURATION
# ============================================================================
# User and group for running the system service (security)
SERVICE_USER=nobody
SERVICE_GROUP=daemon

# Service description for systemd/OpenRC
SERVICE_DESCRIPTION=Kafka Collector - HTTP to Kafka Bridge

# Documentation URL for the service
SERVICE_DOCUMENTATION=https://github.com/kundan/kafka-rust